/* ==========================================================================
   КОНВЕЙЕР КАТИ — КАСТОМНЫЕ СКРИПТЫ ДЛЯ TILDA
   Автоматически подключаемый JS-файл.
   Служит для интерактивных калькуляторов, аналитики и перехвата лидов.
   ========================================================================== */

(function() {
  'use strict';
  console.log('[Конвейер Кати] Скрипт кастомизации успешно загружен.');

  // Инициализация после загрузки DOM
  document.addEventListener('DOMContentLoaded', function() {
    initFormHandlers();
  });

  // Отслеживание успешной отправки форм Тильды
  function initFormHandlers() {
    document.addEventListener('tildaform:aftersuccess', function(e) {
      var form = e.target;
      console.log('[Конвейер Кати] Форма успешно отправлена:', form);
      
      // Здесь можно автоматически отправлять событие в локальный API/буфер конвейера
    });
  }
})();
